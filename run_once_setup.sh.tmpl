#!/usr/bin/env bash

set -e


# Add mise activation to zsh
if [ ! -f ~/.zshrc ] || ! grep -q "mise activate" ~/.zshrc; then
    echo 'eval "$(mise activate zsh)"' >> ~/.zshrc
fi

echo "✓ Mise configured for zsh"
echo "Please log out and back in for changes to take effect."
